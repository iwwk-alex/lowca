import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.lowca.app',
  appName: 'Łowca',
  webDir: 'dist/poland-discounts-frontend/browser'
};

export default config;
